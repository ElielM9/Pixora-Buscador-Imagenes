/* Constantes y variables */

// Constantes del DOM
const SEARCHER_FORM_ID = document.querySelector(`#searcher-form`);
const RESULTS_CONTAINER_ID = document.querySelector(`#results-container`);
const REGISTERS_PER_PAGE = 40;
const PAGINATION_CONTAINER_ID = document.querySelector(`#pagination-container`);

/* Exportaciones */
export { RESULTS_CONTAINER_ID, SEARCHER_FORM_ID, REGISTERS_PER_PAGE, PAGINATION_CONTAINER_ID };
